{
  "name": "saint-of-the-day",
  "version": "1.0.0",
  "description": "Simple REST webservice using Express.js for AWS Lambda.",
  "main": "dist/handler.js",
  "scripts": {
    "test": "jest",
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "build": "npm test && node scripts/build.js"
  },
  "dependencies": {
    "aws-serverless-express": "^3.4.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "nodemon": "^3.0.0",
    "supertest": "^6.3.0"
  }
}
